{
  "name": "tumblbug-delivery",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "yarn workspace client start && yarn workspace server dev",
    "lint": "yarn workspace client lint && yarn workspace server lint",
    "start": "yarn workspace client start && yarn workspace server start",
    "build": "yarn workspace client build && yarn workspace server build"
  },
  "license": "MIT"
}